\documentclass[$if(fontsize)$$fontsize$,$endif$$if(lang)$$lang$,$endif$$for(classoption)$$classoption$$sep$,$endfor$,parskip=half]{$documentclass$}

$for(header-includes)$
  $header-includes$
$endfor$

% fix for pandoc 1.14
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\makeatletter
\renewcommand{\maketitle}{
  \begin{titlepage}
    \begin{flushleft}

      \URCornerWallPaper{0.32}{/usr/local/pandoc/template/cloudogu/images/cloudogu-logo.pdf}
      \ThisULCornerWallPaper{1.0}{/usr/local/pandoc/template/cloudogu/images/clouds-on-a-string.pdf}
      
      \vspace*{11.1cm}

      \color{maincolor}

      \@author \\
      \@title \\
      \vspace*{0.3cm}
      \@date

      
    \end{flushleft}
  \end{titlepage}
}

\begin{document}

  $if(title)$
    \title{\Huge \textbf{$title$}}
    \author{\Large \textbf{$for(author)$$author$$sep$ \and $endfor$}}
    \date{\small \textbf{$date$}}
    \maketitle
  $endif$

  $if(toc)$
  {
    \clearpage
    % links in toc shouldn't be colored
    \hypersetup{
      urlcolor=black,
      linkcolor=black
      }

    \begingroup
      \renewcommand*{\chapterpagestyle}{empty}
      \pagestyle{empty}
      \tableofcontents
      \clearpage
    \endgroup
  }
  $endif$

  % color links
  \hypersetup{
    urlcolor=maincolor,
    linkcolor=maincolor
    }

  $body$

  \newcommand{\makeback}{
    \clearpage
    \begingroup
      \renewcommand*{\chapterpagestyle}{empty}
      \pagestyle{empty}
      \begin{flushright}

        \URCornerWallPaper{0.32}{/usr/local/pandoc/template/cloudogu/images/cloudogu-logo.pdf}
        
        \vspace*{17cm}

        Cloudogu GmbH \\
        Brabandtstr. 9-10 \\
        D-38100 Braunschweig

        \vspace*{0.3cm}

        hello@cloudogu.com \\
        www.cloudogu.com
        
      \end{flushright}
    \clearpage
    \endgroup
  }
  \makeback
  
\end{document}
