\documentclass[$if(fontsize)$$fontsize$,$endif$$if(lang)$$lang$,$endif$$for(classoption)$$classoption$$sep$,$endfor$]{$documentclass$}
\usepackage{lastpage}
\usepackage[headtopline,headsepline]{scrpage2}

% Makes the entries in the header non italic
\setkomafont{pagehead}{\normalfont\normalcolor}

% ITZ-Bund header: Left
\ihead{
  \smallskip
  \begin{tabular}{@{}l@{}}
    \begin{picture}(50,40)(0,0)
      \put(0,0){
        \includegraphics[width=50pt]{/usr/local/pandoc/template/itz-bund/images/itzbund-logo.png}
      }
    \end{picture}
  \end{tabular}
  \hspace{5mm}
}
% ITZ-Bund header: Middle
\chead{
  \smallskip
  \begin{tabular}{@{}l@{}}
    {\fontsize{12}{12}\selectfont \textbf{$title$}} \\
  {\fontsize{9}{9}\selectfont $author$}
  \end{tabular}
  \hspace{5mm}
}
% ITZ-Bund header: Right
\ohead{
  \smallskip
  \begin{tabular}{@{}ll@{}}
    {\fontsize{8}{8}\selectfont \textbf{Version:}} & {\fontsize{8}{8}\selectfont $version$} \\
    {\fontsize{8}{8}\selectfont \textbf{Stand:}}   & {\fontsize{8}{8}\selectfont $date$ }
  \end{tabular}
  \hspace{5mm}
}

%Activate page numbers
\cfoot[\pagemark]{\vspace{1,25cm} {\fontsize{10}{10}\selectfont \color{black} - Seite \thepage\ von \pageref{LastPage}} -}

% Disable section, chapter, and subsection numbering
\setcounter{secnumdepth}{-1}

% Enable ITZ-Bund Header forl Chapters
\renewcommand{\chapterpagestyle}{scrheadings}
$for(header-includes)$
  $header-includes$
$endfor$

% fix for pandoc 1.14
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%%%
%%% Start: ITZ-Bund title page
%%%
\makeatletter
\renewcommand{\maketitle}{
  \begin{titlepage}
    \begin{picture}(0,0)(0,0)
    \put(30,-30){\includegraphics[width=120pt]{/usr/local/pandoc/template/itz-bund/images/itzbund-logo.png}}
    \end{picture}
    \begin{tikzpicture}[remember picture,overlay]
      \node (a) at ([yshift=3.175cm]current page.west) {};
      \node (b) at ([yshift=-3.175cm]current page.east) {};
      \filldraw[draw=maincolor, fill=maincolor] (a) rectangle (b);
      \node [anchor=south east] (t) at ([xshift=-60pt,yshift=10pt]current page.east) {%
        \begin{varwidth}{\textwidth}
        {\color{white} \@title}
        \end{varwidth}
      };
      \node [anchor=north east] (t) at ([xshift=-60pt,yshift=-10pt]current page.east) {{\color{white} \@author}};
    \end{tikzpicture}
  \end{titlepage}
}

%%%
%%% Start: Structure for document
%%%
\begin{document}
  \thispagestyle{scrheadings}
  \pagestyle{scrheadings}

  $if(title)$
  \title{{\fontsize{35}{1}\selectfont $title$}}
  \author{{\fontsize{15}{1}\selectfont $for(author)$$author$$sep$ \and $endfor$}}
  \date{\small \textbf{$date$}}
  \maketitle
  $endif$

% table of content
  $if(toc)$
    {
    \clearpage
    \begingroup
    \doublespacing
    % Set all entires at the same depth
    \setcounter{tocdepth}{0}
    \tableofcontents
    \singlespacing
    \clearpage
    \endgroup
  }
  $endif$

  $body$

\end{document}