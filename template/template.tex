\documentclass[$if(fontsize)$$fontsize$,$endif$$if(lang)$$lang$,$endif$$for(classoption)$$classoption$$sep$,$endfor$]{$documentclass$}

$for(header-includes)$
  $header-includes$
$endfor$

% fix for pandoc 1.14
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

\makeatletter
\renewcommand{\maketitle}{
  \begin{titlepage}
    \begin{flushleft}

      \URCornerWallPaper{0.32}{/usr/local/pandoc/template/images/cloudogu-logo.pdf}
      \ThisULCornerWallPaper{1.0}{/usr/local/pandoc/template/images/clouds-on-a-string.pdf}
      
      \vspace*{11.1cm}

      \color{maincolor}

      \@author \\
      \@title
      
    \end{flushleft}
  \end{titlepage}
}

\begin{document}

  $if(title)$
    \title{\Huge \textbf{$title$}}
    \author{\Large \textbf{$for(author)$$author$$sep$ \and $endfor$}}
    \maketitle
  $endif$

  $if(toc)$
  {
    \clearpage
    \begingroup
      \renewcommand*{\chapterpagestyle}{empty}
      \pagestyle{empty}
      \tableofcontents
      \clearpage
    \endgroup
  }
  $endif$

    $body$
  
\end{document}
