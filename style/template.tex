\documentclass[$if(fontsize)$$fontsize$,$endif$$if(lang)$$lang$,$endif$$for(classoption)$$classoption$$sep$,$endfor$]{$documentclass$}

$for(header-includes)$
  $header-includes$
$endfor$

% fix for pandoc 1.14
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Set default font to NeoSansPro
\setmainfont[Path=fonts/]{NeoSansPro-Medium.otf}

\newfontfamily\headingfont[Path=fonts/]{NeoSansPro-Medium.otf}
% \titleformat{\chapter}
%    {\LARGE\headingfont \color{maincolor}}{\thechapter}{20pt}{\LARGE\headingfont}
% \titleformat{\section}{\Large\headingfont \color{maincolor}}
% \titleformat*{\subsection}{\large\headingfont \color{maincolor}}

\makeatletter
\renewcommand{\maketitle}{
  \begin{titlepage}
    \begin{flushleft}

      \URCornerWallPaper{0.32}{style/images/cloudogu-logo.pdf}
      \ThisULCornerWallPaper{1.0}{style/images/clouds-on-a-string.eps}
      
      \vspace*{11.1cm}

      \color{maincolor}

      \@author \\
      \@title
      
    \end{flushleft}
  \end{titlepage}
}

\begin{document}

  $if(title)$
    \title{\Huge\headingfont $title$} 
    \author{\Large\headingfont $for(author)$$author$$sep$ \and $endfor$}
    \maketitle
  $endif$

  $if(toc)$
  {
    \clearpage
    \begingroup
%       \renewcommand*{\chapterpagestyle}{empty}
      \pagestyle{empty}
      \tableofcontents
      \clearpage
    \endgroup
  }
  $endif$

  $body$

\end{document}
